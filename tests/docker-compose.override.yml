version: "3.7"
services:
  backend:
    environment:
      HOOKSNIFF_JWT_SECRET: "x"
      HOOKSNIFF_ENDPOINT_HTTPS_ONLY: "false"
      HOOKSNIFF_WHITELIST_SUBNETS: "[127.0.0.1/32, 172.17.0.0/16]"
  pgbouncer:
    ports:
      - "8079:5432" # Needed for sqlx
  redis:
    ports:
      - "8078:6379" # Needed for sqlx
